# Swagger desabilitado em produção
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# JWT - segredo deve vir de variável de ambiente
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=86400000
